initialization
initializeWidgets
	inputLabel := self newLabel.
	input := self newTextInput.
	start := self newButton.
	stop := self newButton.
	step := self newButton.
	reset := self newButton.
	remainderLabel := self newLabel.
	symbolLabel := self newLabel