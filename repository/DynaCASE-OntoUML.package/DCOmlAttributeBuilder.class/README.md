I am a parser / builder that parses textual representation of an attribute and creates a Attribute instance.

!! Todo
There is code-duplicity with DCOmlMethodBuilder.
move it to separate parser or wherever.