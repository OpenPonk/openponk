A DCPaletteGroupModel is class which represents the part of palette (DCPaletteModel) with something incommon, e.g. types of states.

Instance Variables
	items:		<Object>
	name:		<Object>

items
	- the collection of DCPaletteItemModels

name
	- xxxxx
