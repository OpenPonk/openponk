"
I am fired when a model (element) or some aspect of it has changed.

A subscriber to this event should presume that any aspect has changed.
"
Class {
	#name : #OPModelChanged,
	#superclass : #OPModelAnnouncement,
	#category : #'OpenPonk-Core-Announcements'
}
